# MT5 credential profiles — copy this to mt5-credentials.yaml and fill in your values.
# mt5-credentials.yaml is git-ignored. Never commit real credentials.
#
# In config.yaml, set:
#   mt5:
#     use: exness        <- picks the "exness" profile below
#
# Required fields per profile:
#   login                    -- MT5 account number (non-zero integer)
#   password                 -- MT5 account password
#   server                   -- broker server name (shown in MT5 login dialog)
#   terminal_path            -- full path to terminal64.exe for this broker
#   broker_time_offset_hours -- broker server time vs UTC (e.g. UTC+3 -> 3)
#
# Optional:
#   config         -- path to a broker-specific strategy config file.
#                     Deep-merged on top of config.yaml at startup.
#                     Omit entirely if you do not need per-broker overrides.
#   symbol_aliases -- map canonical symbol names to broker-specific names.
#                     The engine always uses canonical names (US100, XAUUSD …);
#                     aliases are applied only when fetching from MT5.

exness:
  login: 0
  password: ""
  server: "Exness-MT5Real5"
  terminal_path: "C:\\Program Files\\MetaTrader 5 EXNESS\\terminal64.exe"
  broker_time_offset_hours: 3
  config: zconfig/exness.yaml
  symbol_aliases:
    US100: USTEC_x100m   # Exness names the Nasdaq index USTEC_x100m

icmarkets:
  login: 0
  password: ""
  server: "ICMarketsSC-MT5-2"
  terminal_path: "C:\\Program Files\\ICMarkets MetaTrader 5\\terminal64.exe"
  broker_time_offset_hours: 2
  config: zconfig/icmarkets.yaml

fbs:
  login: 0
  password: ""
  server: "FBS-Demo"
  terminal_path: "C:\\Program Files\\FBS MetaTrader 5\\terminal64.exe"
  broker_time_offset_hours: 3
  config: zconfig/fbs.yaml

# Profile with no broker-specific overrides -- omit the config key entirely.
fundednext:
  login: 0
  password: ""
  server: "FundedNext-Server 3"
  terminal_path: "C:\\Program Files\\FundedNext MT5 Terminal\\terminal64.exe"
  broker_time_offset_hours: 3
